import React from 'react';
import classnames from 'classnames';

const ContactInfo = ({ first, last, email, }) => {
    return(
        <div>
            {children}
        </div>
    )
}
export default ContactInfo;
